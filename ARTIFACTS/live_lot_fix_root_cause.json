{
  "job": "FXG_FIX_MT5_0_01_LOT_FALLBACK_ON_LIVE_CONSUMER_V1",
  "phase": "3_prove_root_cause_of_0_01_fallback",
  "primary_root_cause": "FTMO_Bridge_EA never read the canonical JSON 'lots' field; it derived volume from units*0.0001 and applied MathMin(InpMaxLotSize, ...) with default InpMaxLotSize=0.01, forcing a hard cap at 0.01 even when ALPHA emitted larger effective size via lots or when units implied >0.01.",
  "evidence_paths": [
    "ea/FTMO_Bridge_EA.mq5 (pre-change behavior described in git; replaced at lines previously containing MathMin(InpMaxLotSize,...))",
    "docs/bridges/WINDOWS_CANONICAL_EA_CUTOVER.md (documents InpMaxLotSize default 0.01 as max lots per trade)"
  ],
  "secondary_factors": [
    "Legacy MathMax(0.01, ...) floor prevented sub-0.01 exploration but was not the dominant cap versus InpMaxLotSize",
    "Signals without lots will now be rejected (explicit) rather than implied from units"
  ],
  "distinguished_from": "ALPHA emit corruption (out of scope per job; live_truth states ALPHA lots were already valid on canonical lines)"
}
