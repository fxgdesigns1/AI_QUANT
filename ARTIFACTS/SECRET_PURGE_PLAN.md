# Secret Purge Plan - Working Tree Cleanup

## Files Containing Hardcoded Secrets

Based on the security scan, the following files contain hardcoded secrets and require action:

| File | Action | Rationale | Replacement Mechanism |
|------|--------|-----------|----------------------|
| `DIRECT_STRATEGY_TEST.py` | DELETE | Legacy test file with hardcoded key | Use env-only test setup |
| `FIX_API_CREDENTIALS_NOW.md` | DELETE | Documentation with leaked secrets | Replace with template docs |
| `check_12hour_trades.py` | REFACTOR | Active script with fallback | Remove fallback, use strict env |
| `structure_inventory.json` | DELETE | Inventory file with secrets | Regenerate without secrets |
| `comprehensive_trading_system.py` | REFACTOR | Sets env var with hardcoded value | Remove hardcoded assignment |
| `test_complete_system.py` | REFACTOR | Test with hardcoded env assignment | Use mock/fixture approach |
| `strategy_performance_dashboard.html` | REFACTOR | HTML with embedded API key | Remove embedded key, use backend |

### Google Cloud Trading System Directory (MASS DELETE)
The entire `google-cloud-trading-system/` directory contains 25+ files with hardcoded secrets:
- All `app*.yaml` files (GAE configs with secrets)
- All strategy Python files with fallback keys  
- All YAML configs with embedded secrets

**Action**: DELETE entire `google-cloud-trading-system/` directory - it's legacy/backup code not used by current system.

### Files to Keep and Refactor

| File | Current Issue | Fix Required |
|------|---------------|--------------|
| `check_12hour_trades.py` | `os.environ.get('OANDA_API_KEY', 'c01de...')` | Remove fallback, use `settings.oanda_api_key` |
| `comprehensive_trading_system.py` | `os.environ['OANDA_API_KEY'] = "c01de..."` | Delete hardcoded assignment line |
| `test_complete_system.py` | `os.environ['OANDA_API_KEY'] = "c01de..."` | Use pytest fixture or mock |
| `strategy_performance_dashboard.html` | `const API_KEY = 'c01de...'` | Remove const, fetch from backend |

### Files Already Clean (Reference Only)
These files mention the env var names but don't contain secrets:
- `src/core/settings.py` ✅ Proper env-only access
- `systemd/ai-quant-control-plane.env.example` ✅ Template with placeholders
- Most documentation files ✅ Reference env vars without values

## Execution Plan

### Phase 1: Mass Delete Legacy Files
```bash
# Delete files with no current system value
git rm -f DIRECT_STRATEGY_TEST.py
git rm -f FIX_API_CREDENTIALS_NOW.md  
git rm -f structure_inventory.json
git rm -rf google-cloud-trading-system/
```

### Phase 2: Refactor Active Files
1. `check_12hour_trades.py` - Remove fallback from os.environ.get()
2. `comprehensive_trading_system.py` - Delete hardcoded env assignment
3. `test_complete_system.py` - Replace with proper test fixture
4. `strategy_performance_dashboard.html` - Remove embedded API key

### Phase 3: Verification
```bash
# Confirm no secrets remain in working tree
rg -n "c01de9eb4d79|7248728383:" . || echo "CLEAN"
```

## Safety Constraints

1. **Never commit replacement secrets** - Only use env vars or Secret Manager
2. **No fallback values** - Fail fast if env vars missing
3. **Test after changes** - Ensure refactored code still works
4. **Document env requirements** - Update setup docs with required vars

---
*Generated by security audit on 2026-01-05*