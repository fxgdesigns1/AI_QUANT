
# System and artifacts
.DS_Store
__pycache__/
*.pyc
.env
.env.local_research
venv/
.venv/
.idea/
.vscode/
logs/
backups/
.BACKUPS/
PROBE_OUTPUT.json.*
VERIFICATION_REPORT.json.*
*.log

# Dependencies and artifacts
node_modules/
screenshots/
test-results/
sandbox/
reports/
runtime/
forensic_snapshot/
archives/
google-cloud-credentials/
tests/playwright/.cache/

# Local-only secrets and env backups (never commit)
.env.bak
AUTH_CODE.txt

# GCP service-account keys — real private keys, never commit (re-added 2026-06-12
# after a .gitignore rewrite dropped the old *.json credential rules)
Key/
cloud_declutter_v2/keys/
**/keys/*.json

# Machine-local research outputs (untracked new paths only; already-tracked ARTIFACTS remain tracked)
ARTIFACTS/monte_carlo/
ARTIFACTS/exports/pack_*/
ARTIFACTS/backtests/tournament_*_clean/
